const notes = [{
    title: 'My next trip',
    body: 'I would like to go to Spain'
}, {
    title: 'Habits to work on',
    body: 'Exercise. Eating a bit better.'
}, {
    title: 'Office modification',
    body: 'Get a new seat'
}]

// console.log(notes.pop())
// notes.push('My new note')

// console.log(notes.shift()) // remove end
// notes.unshift('My first note') // beginning

//notes.splice(1, 1) // start at 1 and remove 1
//notes.splice(1, 1, 'This is the new second item')

//notes[2] = 'This is now the new note 3 '

console.log(notes.length)
console.log(notes)

// reversedList = notes.toReversed()
// console.log(reversedList) // grabs last item from the list


// notes.forEach(function (item, index) { // gets called for each item in the array
//     console.log(index)
//     console.log(item)
// })

notes.indexOf('Note 2') // returns items posiiton in array

//notes

// for (initializer, condition, final expression)
