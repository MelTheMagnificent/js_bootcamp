let add = function (a, b) {
    return a + b
}

let result = add(10, 1)
//console.log(result)

console.log("Mel") // No difference between double quotes and single quotes

let name = 'Andrew'
console.log(`Hey, my name is ${name}!`) //template string

// template string vs normal string - only a syntax difference, template string makes it look cleaner